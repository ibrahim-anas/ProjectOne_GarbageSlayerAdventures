/// @description cheat codes

//cheat code to increase the player speed
if (keyboard_check(ord("Z"))) {
    move_speed += 1
}

//cheat code to increase firerate of water gun
if (keyboard_check(ord("F"))) {
    firerate = 10
}

//cheat code to restore player health
if (keyboard_check(ord("H"))) {
    health = 200
}